# LocalBusiness JSON-LD Schema — 2026-05-18

## Geänderte Dateien

- `index.html`
- `reports/codex-schema-localbusiness.md`

## Untracked Dateien

- `reports/codex-schema-localbusiness.md`

## Was wurde geändert

- `index.html`
  - LocalBusiness JSON-LD direkt vor `</head>` ergänzt.
  - Schema enthält Name, Beschreibung, URL, E-Mail, Adresse, Founder, `areaServed` und `priceRange` gemäß Vorgabe.

## Was wurde bewusst nicht geändert

- Keine sichtbaren Inhalte geändert.
- Keine Styles, Links, Skripte oder Assets geändert.
- Kein Commit, kein Push, kein Deployment.

## Offene Fragen für Human Review

- Keine.

## Asset- und Lizenzhinweise

- Keine neuen Assets, Schriften oder externen Ressourcen hinzugefügt.

## Lighthouse-Score

- Nicht neu gemessen, da nur ein kleiner JSON-LD-Block ergänzt wurde.
- JSON-LD wurde lokal aus `index.html` extrahiert und erfolgreich als JSON geparst.
