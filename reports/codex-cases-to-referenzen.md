# Cases zu Referenzen

Datum: 2026-05-18

## Geänderte Dateien

- `index.html`
- `cases.html`
- `cases/lingqi.html`
- `blog/index.html`
- `blog/google-bewertung-loeschen.html`
- `blog/lingqi-haarausfall.html`
- `audit-beispiel.html`
- `danke.html`
- `reports/codex-cases-to-referenzen.md`

## Untracked Dateien

- `reports/codex-cases-to-referenzen.md`

## Was wurde geändert

- Vor der Änderung wurde `AGENTS.md` gelesen.
- Danach wurde `git pull origin main` ausgeführt. Ergebnis: Repository war bereits aktuell.
- Sichtbare Navigationstexte `Cases` wurden in den genannten Dateien zu `Referenzen` geändert.
- Sichtbare Einzeltexte wie `Aktueller Case`, `Case Übersicht`, `Case im Detail lesen`, `LingQi Case`, `LingQi Case Study`, `Zur Case-Übersicht`, `Worum es in diesem Case geht` und `Erfolgscase` wurden sinnvoll zu Referenz-Formulierungen geändert.
- `href`-Attribute, URLs, Dateinamen, Canonical-Links und CSS-Klassen mit `case`/`cases` wurden bewusst nicht verändert.

## Was wurde bewusst nicht geändert

- Keine Dateinamen, Verzeichnisse oder Links wie `href="cases/..."`.
- Keine CSS-Klassen wie `case-card` oder `case-study`.
- Keine Canonical-URLs wie `https://maxcontentseo.de/cases.html`.
- Die im Auftrag genannten sichtbaren Texte `Aktuelle Cases` und `Weitere Cases folgen` waren im aktuellen Repo nicht vorhanden.

## Offene Fragen für Human Review

- Bitte prüfen, ob zusätzlich deutsche Begriffe wie `Fallstudie` künftig ebenfalls komplett auf `Referenz` umgestellt werden sollen. In diesem Run wurden nur sichtbare `Case`/`Cases`-Begriffe und direkte Case-Formulierungen geändert.

## Asset- und Lizenzhinweise

- Keine Assets, Bilder, Fonts oder externen Quellen verwendet.

## Lighthouse-Score

- Nicht gemessen, da nur sichtbare Textlabels geändert wurden.
- Lokale Prüfung: Alle betroffenen Seiten lieferten HTTP 200:
  `/`, `/cases.html`, `/cases/lingqi.html`, `/blog/`, `/blog/google-bewertung-loeschen.html`, `/blog/lingqi-haarausfall.html`, `/audit-beispiel.html`, `/danke.html`.
