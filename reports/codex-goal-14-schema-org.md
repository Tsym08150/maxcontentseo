# Codex Goal 14: Schema.org ProfessionalService — 2026-05-21

## Geänderte Dateien

- `index.html`
- `cases/index.html`
- `branchen/kosmetikstudios.html`

## Untracked Dateien

- `reports/codex-goal-14-schema-org.md`

## Was wurde geändert

- `index.html`: vorhandenes `LocalBusiness`-JSON-LD durch ein `@graph`-Schema mit `ProfessionalService` und `Person` ersetzt.
- `cases/index.html`: identisches `ProfessionalService`- und `Person`-JSON-LD direkt vor `</head>` eingefügt.
- `branchen/kosmetikstudios.html`: identisches `ProfessionalService`- und `Person`-JSON-LD direkt vor `</head>` eingefügt.

## Was wurde bewusst nicht geändert

- Keine weiteren Schema-Typen ergänzt.
- Keine bestehenden Inhalte, CTAs, Layouts oder Styles geändert.

## Offene Fragen für Human Review

- Keine.

## Asset- und Lizenzhinweise

- Keine neuen Assets, Schriften, Bilder oder externen Ressourcen hinzugefügt.

## Lighthouse-Score

- Nicht gemessen; Änderung ist nicht performance-relevant.
