# Pricing-Restructure und Referenzen-Texte — 2026-05-18

## Geänderte Dateien

- `index.html`
- `reports/codex-pricing-restructure.md`

## Untracked Dateien

- `reports/codex-pricing-restructure.md`

## Was wurde geändert

- `index.html`
  - Pricing-Sektion in zwei Blöcke umgebaut:
    - `Einmaliger Einstieg` mit Premium-Audit und Quick Fix.
    - `Laufende Betreuung` mit SEO-Betreuung und Premium.
  - Premium-Audit auf `1.299€` aktualisiert.
  - Quick Fix auf `699€` aktualisiert.
  - Alte Referenzaktion, Durchstreich-/Statt-Preis und Quick-Fix-Fußnote entfernt.
  - Premium-Audit als empfohlene Karte mit Featured-Border markiert.
  - Trennlinie zwischen Einstieg und laufender Betreuung ergänzt.
  - Fußnote auf `Keine Mindestlaufzeit · Alle Preise zzgl. MwSt.` reduziert.
  - Hero-CTA-Text von `Fallstudie ansehen` zu `Referenz ansehen` geändert.
  - Sichtbare Audit-Einstieg-Preise auf der Startseite auf `1.299€` angepasst.
  - Sichtbare `Cases`-Texte waren nach Pull bereits als `Referenzen` vorhanden; die Prüfung ergab keine verbleibenden sichtbaren Treffer in den angefragten Dateien.

## Was wurde bewusst nicht geändert

- Keine `href`-Attribute, URLs, Dateinamen oder CSS-Klassen wegen der Cases/Referenzen-Umbenennung geändert.
- Keine Dateien außerhalb des angefragten Scopes geändert.
- Kein Commit, kein Push, kein Deployment.

## Offene Fragen für Human Review

- Bitte kurz visuell prüfen, ob die neue zweigeteilte Preisstruktur exakt dem gewünschten Muster entspricht.

## Asset- und Lizenzhinweise

- Keine neuen Bilder, Fonts, Icons oder externen Assets hinzugefügt.

## Lighthouse-Score

- Nicht neu gemessen, da keine Performance-relevanten Assets oder Skripte hinzugefügt wurden.
- Lokale Prüfung: `http://127.0.0.1:8080/` liefert HTTP 200.
- Lokale Inhaltsprüfung bestätigt die neuen Preis- und CTA-Texte.
- Chrome-Headless-Render wurde erfolgreich erzeugt.
