# Goal 06b — Hero Layout Fix, 15.05.2026

## Geänderte Dateien
- `index.html`
- `reports/codex-goal-06b-hero-layout-fix.md`

## Untracked Dateien
- `reports/codex-goal-06b-hero-layout-fix.md`

## Was wurde geändert
- Hero-H1-Schriftgröße von `clamp(2.55rem, 5.5vw, 4.85rem)` auf `clamp(2.2rem, 4.5vw, 3.8rem)` reduziert.
- Hero-Grid von `minmax(0, 1.05fr) minmax(280px, 0.95fr)` auf `minmax(0, 1.15fr) minmax(240px, 0.85fr)` angepasst.
- Hero-Foto-Breite von `min(100%, 390px)` auf `min(100%, 340px)` reduziert.

## Was wurde bewusst nicht geändert
- Kein HTML.
- Keine weiteren CSS-Werte.
- Keine anderen Sektionen.

## Offene Fragen für Human Review
- Keine.

## Asset- und Lizenzhinweise
- Keine Assets betroffen.

## Lighthouse-Score
- Nicht gemessen; reine CSS-Wertkorrektur.
