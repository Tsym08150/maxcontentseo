# Hero Trust und Georg-Box — 2026-05-18

## Geänderte Dateien

- `index.html`
- `reports/codex-hero-trust-georg.md`

## Untracked Dateien

- `reports/codex-hero-trust-georg.md`

## Was wurde geändert

- `index.html`
  - Direkt unter den Hero-CTAs drei Proof-Points ergänzt:
    - `48h Ersteinschätzung`
    - `Kein Agentur-Vertrag`
    - `Konkrete Maßnahmen statt Theorie`
  - Trust-Bar im Hero auf vier Felder aktualisiert:
    - `Referenz` / `LingQi TCM München`
    - `Audit-Einstieg` / `499€ · ohne Vertrag`
    - `Lieferzeit` / `48h Analyse`
    - `Spezialisierung` / `Beauty & Wellness`
  - Neue Georg-Sektion nach dem Hero ergänzt:
    - Label `Wer Ihre Analyse macht`
    - H2 `Kein Team. Kein Übergabe-Risiko.`
    - Subtext zur spezialisierten Solo-Arbeit
    - Hellgraue Box mit rundem `GS`-Avatar und Beschreibung von Georg Stopfer.
  - Lokale CSS-Regeln für Proof-Points, Trust-Bar-Textgröße und Georg-Box ergänzt.

## Was wurde bewusst nicht geändert

- Keine anderen Seiten geändert.
- Keine externen Assets, Fonts, Icons oder Skripte hinzugefügt.
- Keine bestehenden Links oder Formulare geändert.
- Kein Commit, kein Push, kein Deployment.

## Offene Fragen für Human Review

- Bitte visuell prüfen, ob die Proof-Points in der Hero-Zeile auf Mobilgeräten eng genug, aber noch lesbar wirken.

## Asset- und Lizenzhinweise

- Keine neuen Assets verwendet. Der Avatar ist ein CSS/Text-Element mit den Initialen `GS`.

## Lighthouse-Score

- Nicht neu gemessen, da keine externen Ressourcen oder Skripte ergänzt wurden.
- Lokale Browser-Prüfung: Startseite lädt, alle Pflichttexte sind im DOM vorhanden, Console-Logs für `error`/`warn` waren leer.
